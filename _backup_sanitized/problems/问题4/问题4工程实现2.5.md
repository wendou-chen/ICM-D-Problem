下面按“**Q4 最终应该交付几张图** → **你当前工程里到底已经产出了哪些图（按文件链路逐一对照）** → **哪些图必须增强/补齐 + 需要改什么代码/数据**”给你一个清晰的结论与整改清单。

---

## 1) Q4 最终到底输出几张图最合适？

结合题面 Q4 的要求（比较不同方案的环境影响，并说明如何调整模型最小化影响）【】，你们的“**O 奖级最稳配置**”是：

### ✅ 核心 6 图（建议作为正文证据链）

1. **漏桶动力学图**：$S_t$（平流层黑碳库存）时间序列 + 阈值线（可加发射频次辅助轴）
2. **环境分量堆叠柱状图**：Scenario A/B/C 的 $E_{\mathrm{CO2}}^{R}$、$E_{\mathrm{CO2}}^{E}$、$E_{\mathrm{LCA}}$
3. **Cost–EDI Pareto**：经济–环境权衡（最好带 Feasible/Infesible 标记）
4. **Time–EDI Pareto**：工期–环境权衡（这是题意核心，“快但脏 vs 慢但绿”）
5. **$\chi$ 敏感性图**：去碳化旋钮对排放的影响（回答“如何调整模型”）
6. **环境尾部风险图**：箱线图/分位数图展示 $S_{\max}$ 或 EDI 的 VaR/CVaR（把 Q2/Q3 的随机性优势迁移进 Q4）

> 若篇幅紧：**最少 4 图能写完 Q4**（你当前就是这套），但为了“不可挑刺”，建议补齐 **Time–EDI + Tail Risk**（这两张是评委最容易追问的缺口）。

---

## 2) 你现在工程里已经实现了哪些图？（按文件对照）

你目前有两条“Q4 产图管线”，它们输出的图并不完全相同：

---

### A) `run_q4_env_sweep.py` + `viz_q4_results.py` 这条管线：**已产出 4 张图（你说的那 4 张）**

`viz_q4_results.py` 明确生成并保存：

* `fig_q4_env_components.png`（堆叠柱）
* `fig_q4_soot_timeseries.png`（$S_t$ 曲线，但注意它是“把全年总 attempts 平均摊到 365 天”的再仿真近似）
* `fig_q4_pareto_edi_cost.png`（Cost–EDI，且 cost 是 proxy）
* `fig_q4_chi_sensitivity.png`（$\chi$ 曲线）
  并且把 plot_data 也导出 csv。
  【】

同时 `run_q4_env_sweep.py` 产出 `outputs/q4/summary_q4_sweep.csv`，但它的仿真基底来自 Q3 的水库存模型（`simulate_inventory_trajectory(duration_days=365*5, ...)`），并且固定跑 5 年，用于服务水平与环境记账。
【】

✅ 结论：**你现在“4 图”确实来自这一条管线。**

---

### B) `run_q4_detailed_sim.py` + `viz_q4_detailed.py` 这条管线：**其实已经有能力产出 Time–EDI + Tail Risk（但你可能还没纳入主报告）**

`run_q4_detailed_sim.py` 是“运送 $M=10^8$ 吨直到完成”的运输仿真（更贴合 Q1/Q4 的建材运输主线），并且做了 Monte Carlo（`N_MC=5`）。它输出：

* `outputs/q4_detailed/mc_results.csv`
* `outputs/q4_detailed/summary_stats.csv`
  【】

`viz_q4_detailed.py` 会读取 `mc_results.csv` 并生成：

* `fig_q4_pareto_time_edi.png`（Time–EDI Pareto ✅）
* `fig_q4_soot_risk.png`（S_max 箱线图 ✅ = Tail Risk 的一种）
* `fig_q4_impact_breakdown.png`（按 Scenario 分类的 CO2 分量柱图，属于“分量图的另一版本”）
  此外还尝试画 `fig_q4_soot_traces_detailed.png`，但前提是存在 `traces.csv`（而当前 `run_q4_detailed_sim.py` 没有把 trace 写成 `traces.csv`，所以通常会跳过）。
  【】【】

✅ 结论：**你并不是“只能做 4 张图”**；你第二条管线已经覆盖了你缺的两张（Time–EDI + Tail Risk），只是目前没有统一成“Q4 最终交付套件”。

---

## 3) Q4 最终应输出哪几张图？哪些需要增强？

### 3.1 最终建议：以“6 张核心图”为目标，但要先统一口径（很关键）

现在两条管线的最大问题不是“缺图”，而是**口径不一致**：

* `run_q4_env_sweep.py` 是“水保障供应链 + 环境记账”（偏 Q3 逻辑）【】
* `run_q4_detailed_sim.py` 是“建材运输直到 $M$ 完成 + 环境记账”（偏 Q1/Q4 主任务）【】

而题面 Q4 讨论的是“实现 10 万人月球城建设的环境影响”，逻辑上应主要基于**建材运输达成 $M=10^8$ 吨**的过程【】。
所以正文的 6 张图建议以 **detailed_sim 管线为主**，env_sweep 管线的 4 张图可以保留其中“构图形式”，但数据最好来自“建材运输口径”的结果。

---

## 4) 你目前实现了哪些图？还要改进什么？（按图逐项给整改点）

下面我按“6 张最终图”逐项对照你现状，并明确“增强/新增/替换”的建议。

---

### 图 1：漏桶动力学 $S_t$（需要增强）

* 你已产出：`fig_q4_soot_timeseries.png`（来自 `viz_q4_results.py`）【】
* 但它存在一个硬伤：它不是用真实 daily attempts 轨迹，而是把全年总 attempts **均匀摊到 365 天**再跑漏桶（`daily_attempts = total_attempts/365`）【】。这会抹掉 burst/应急导致的“尖峰”，叙事力下降。

✅ 建议增强为你之前说的“升级版 A”：

* 用 **真实日 attempts** 做右轴柱状（或按年聚合的频次条带背景）
* 画阈值线 $S_{\text{critical}}$
* 对比 Rocket-heavy vs Mixed 两条曲线

**工程改动最小方案：**

* 让 `run_q4_detailed_sim.py` 把 `trace_data` 写入 `outputs/q4_detailed/traces.csv`（目前只在内存里返回 `trace_data`，没落盘）【】
* 在 trace 中同时写 `S_bc` 和 `attempts_today`（现在 trace 只存了 `S_bc`、`mass_pct`，没存 attempts）【】
* 然后用 `viz_q4_detailed.py` 的 trace 绘图段落真正生成 `fig_q4_soot_traces_detailed.png`（现在通常跳过）【】

---

### 图 2：环境分量堆叠柱（建议增强，但不必新增）

* 你已产出：`fig_q4_env_components.png`（sweep 管线）【】
* detailed 管线也产出类似的 `fig_q4_impact_breakdown.png`（按 Scenario 分类的分量柱图）【】

✅ 建议增强点（主要是“口径一致 + 讲清 LCA 是一次性债务”）：

* **统一使用建材运输口径的数据**（detailed_sim）
* LCA 可以注明“Committed Carbon”或“amortized over X years”两种画法（正文用 committed，附录给 amortized）。

---

### 图 3：Cost–EDI Pareto（需要增强）

* 你已产出：`fig_q4_pareto_edi_cost.png`（但 cost 是 `compute_proxy_cost()` 的 proxy）【】
* detailed 管线当前没有 cost 字段（mc_results.csv 里没有成本）【】

✅ 必须增强的原因：如果 cost 是拍的 proxy，评委追问“成本模型在哪？”会很被动。
**整改建议：**

* 从 Q1 成本模块接入：每次 attempts_today 视为一次 launch 成本（并应用学习曲线），电梯按吨×OPEX，Apex 段按 β 折扣（你们 Q1 已有这口径）。
* 最小实现：先把 `C_L` 设为常数，至少让 Cost–EDI 不是“随意的 50M/launch”硬编码。【】

---

### 图 4：Time–EDI Pareto（你已经实现了，建议纳入“最终交付套件”）

* `viz_q4_detailed.py` 已输出 `fig_q4_pareto_time_edi.png`【】
* 这张图是 Q4 最贴题的 tradeoff 证据链，建议作为正文核心图，而不是“额外附图”。

✅ 建议增强点：

* 加 Feasible/Infesible 标记（基于 $S_{\max}\le S_{\text{critical}}$ 与年度发射预算）
* 颜色用 `alpha_actual`，形状用 `completed`（你代码里已经这么做了）【】

---

### 图 5：$\chi$ 敏感性（建议增强为你提的“瀑布图 C”）

* 你已产出：`fig_q4_chi_sensitivity.png`（sweep 管线）【】

✅ 增强建议（非常推荐）：补一张或替换为“瀑布图 Waterfall”，把减排贡献拆成：

1. 引入电梯（火箭排放下降）
2. 引入 LCA（建设碳债上升）
3. 去碳化（电梯运营排放下降）

这张图对回答 “How would you adjust your model to minimize environmental impact?” 极其有用（把 $\chi$ 变成政策旋钮的定量证据）。题面明确要你说明“如何调整模型”。【】

---

### 图 6：Tail Risk（你其实已经实现了，但需要“定位成 Q4 核心图”）

* `viz_q4_detailed.py` 已输出 `fig_q4_soot_risk.png`（按 Scenario 分类的 S_max 箱线图）【】

✅ 建议增强点：

* 把箱线图的标题从“Risk Boxplot”改成 “$\mathrm{VaR}*{0.95}$ / $\mathrm{CVaR}*{0.95}$ of $S_{\max}$” 的叙事
* 由于你目前 `N_MC=5` 偏少，箱线图统计不稳；建议提升到 30（至少 20）以让尾部风险有说服力。【】

---

## 5) 一句话总结：你现在到底还缺什么、该增强什么？

### 你“已经有”的

* 4 张基础图（sweep 管线）✅【】
* Time–EDI + Tail Risk（detailed 管线）✅【】

### 你“还需要统一/改进”的

1. **统一口径**：正文 Q4 图建议以 “运送 $M=10^8$ 吨直到完成”的 detailed_sim 为主【】【】
2. **增强漏桶图**：不要用均匀摊分 attempts 的再仿真；用真实日轨迹 + 阈值 + 发射频次（升级图 A）【】【】
3. **增强 Cost–EDI**：把 proxy cost 替换为 Q1 的成本口径或至少统一成本参数，避免“50M/launch”硬编码被质疑【】
4. **增强 $\chi$ 图**：加瀑布图（升级图 C），直观回答“如何调整模型”【】
5. **提升 N_MC**：把 `N_MC=5` 提高到 ≥20，Tail Risk 才够硬【】

---

如果你愿意，我可以在你这四个脚本的基础上给出一个“**最终 Q4 统一产图入口**”的建议结构（例如 `scripts/run_q4_all.py`：先跑 detailed_sim 输出 mc_results + traces，再跑一个 `viz_q4_final.py` 一次性生成 6 图），并明确每张图读哪个 CSV、需要哪些字段，保证不会再出现“两套管线各画各的、口径对不上”的问题。
