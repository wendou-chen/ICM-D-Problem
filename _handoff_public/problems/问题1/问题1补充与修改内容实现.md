```text
你在一个 MCM B 题 Q1 工程里工作。请读取并修改以下模块以加入“成本–工期”计算与绘图：
已存在文件：
- configs/constants.py （当前 Cost 里 C_LAUNCH_RANGE_USD 与 C_ELEVATOR_PER_TON_USD 是 NaN 占位）
- scripts/run_q1.py （当前只生成 baseline、时间鲁棒、图：累计质量与 time-vs-alpha）

目标：
(1) 将用户提供的成本约束落地到 constants.py；
(2) 实现包含“2050 后每 5 年成本下降 5%”的火箭成本学习曲线；
(3) 在 Q1 pipeline 中输出 cost–time 的 :contentReference[oaicite:10]{index=10}），并同时保留 time-vs-alpha 图。

A. 修改 configs/constants.py
1) 在 Cost dataclass 中新增字段（单位必须写在注释中）：
- ROCKET_LAUNCH_COST_2050_RANGE_USD: (20_000_000, 60_000_000)  # USD/launch
- ROCKET_COST_DECAY_RATE_PER_5YR: 0.05  # dimensionless
- ROCKET_COST_DECAY_PERIOD_YR: 5  # years
- ROCKET_COST_FLOOR_USD: 10_000_000  # USD/launch, do not decay below this
- ELEVATOR_OPEX_PER_KG_RANGE_USD: (100, 500)  # USD/kg
- BETA_APEX_RANGE: (0.2, 0.8)  # apex-to-moon per-ton cost ratio vs direct rocket (robust interval)
2) 保持 Problem/Elevator/Rocket 现有字段不变。

B. 新增 src/q1/cost_model.py（如已有 cost.py 则在其中实现同等功能）
必须实现：
- rocket_cost_per_launch(year:int, C0_usd:float, start_year:int, decay_rate:float, period_yr:int, floor_usd:float) -> float
- total_rocket_cost(n_launches:int, launches_per_year:int, start_year:int, C0_usd:float, decay_rate:float, period_yr:int, floor_usd:float) -> float
实现逻辑：按年分桶，year = start_year + t，成本按 floor(max) 学习曲线计算；每年最多 launches_per_year 次发射，直到 n_launches 用完。

C. 修改 src/q1/baseline.py
- 在 build_q1_baseline_table() 返回的 DataFrame 中新增列：
  cost_low_usd, cost_high_usd
- 计算方式（按每行 scenario）：
  * Scenario B: n_launches = ceil(M / q), launches_per_year = 365*K*r
    cost_low/high 使用 C0_low/high 计算 total_rocket_cost
  * Scenario A: 电梯 OPEX = M * elevator_opex_per_kg * 1000（kg/ton）；
    Apex 段发射次数 n_A = ceil(M / qA)（qA 默认等于 q）；
    Apex 单次成本 = beta * rocket_cost_per_launch(…)；beta 取 beta_low/high；
    Apex 每年发射次数可近似 = ceil(C_E / qA)（与电梯吞吐同步）
  * Scenario C: 对每个 alpha 扫描时需要 cost_low/high：
    cost = (alpha*M)*elevator_opex + apex_cost_for_alpha + rocket_cost_for_(1-alpha)
    并输出 time_years = max(alpha*M/C_E, (1-alpha)*M/C_R)

D. 修改 src/q1/plots.py
新增函数：
- plot_pareto_cost_time_band(df_scan_alpha, out_dir)
要求输出：
- outputs/q1/figs/pareto_cost_time_band.png/pdf/svg
图形要求：
- x=time_years
- y=cost_low_usd 与 cost_high_usd 之间填充阴影带
同时保留已有的 plot_pareto_alpha（time-vs-alpha）。

E. 修改 scripts/run_q1.py
- import Cost（以及 cost_model/baseline 扫描结果）
- 在生成 baseline 后，额外生成 alpha 扫描 DataFrame（至少 0..1 共 101 点）
- 调用 plot_pareto_cost_time_band
- 输出一个新的 CSV：outputs/q1/q1_tradeoff_alpha.csv（含 alpha, time_years, cost_low_usd, cost_high_usd）

验收标准：
1) 运行 scripts/run_q1.py 不报错；
2) outputs/q1/q1_tradeoff_alpha.csv 存在且列齐全；
3) figs 下至少包含：cum_mass_vs_year.*、pareto_alpha.*、pareto_cost_time_band.* 三组图；
4) 成本曲线必须体现学习曲线：如果 time_years 更长，总成本在相同发射次数下不会变得更高得离谱（允许下降或趋于 floor）。